---
applications:
- name: samplesvc-ci
  instances: 2
  host: samplesvc-ci
  memory: 1024M
  path: target/samplesvc.jar
  timeout: 180
  services:
    - rm-pg-db
    - rm-redis
    - rm-rabbitmq
  env:
    partySvc_connectionConfig_host: ras-party-service-ci.apps.devtest.onsclofo.uk
    partySvc_connectionConfig_port: "80"
    partySvc_connectionConfig_scheme: http
