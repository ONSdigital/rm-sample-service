---
applications:
- name: samplesvc-int
  instances: 1
  host: samplesvc-int
  memory: 1024M
  path: samplesvc/target/samplesvc.jar
  timeout: 180
  services:
    - rm-pg-db
    - rm-redis
    - rm-rabbitmq
  env:
    partySvc_connectionConfig_host: ras-party-service-int.apps.devtest.onsclofo.uk
    partySvc_connectionConfig_port: "80"
    partySvc_connectionConfig_scheme: http
