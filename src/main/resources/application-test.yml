liquibase:
  user: postgres
  password: postgres

spring:
  datasource:
    schema: classpath:/schema.sql
    username: postgres
    password: postgres

sample-unit-distribution:
  delay-milli-seconds: 1000